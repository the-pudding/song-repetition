fontify($ptSerif, $ptSerif-hash, $serif)
fontify($roboto, $roboto-hash, $sans)

html, body
	background-color #fff
	color $off-black
	font-size 17px

p 
	line-height 1.7

h1
	font-size 32px
	margin-bottom 30px
h2
	font-size 24px
	margin-bottom 20px
div
	margin-left 10px
